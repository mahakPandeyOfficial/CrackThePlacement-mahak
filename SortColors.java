class Solution {
    public void sortColors(int[] nums) {
      //sort the array
        Arrays.sort(nums);
      //according to sorting in order we will get 0 -> 1 -> 2 as colors we want as a output in questions
        System.out.println(nums);
    }
}
